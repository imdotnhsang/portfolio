pre-commit:
  parallel: true
  commands:
    prettier:
      glob: '*.{js,ts,jsx,tsx,css,scss,json,md}'
      run: npm run format {staged_files} && git add .
    eslint:
      glob: '*.{js,ts,jsx,tsx}'
      run: npm run lint:fix {staged_files}
